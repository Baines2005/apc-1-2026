#include <stdio.h>
#include <math.h>

int main () {
    // 1.Entrada
    int operador1;
    int operador2;

    printf("Entre com um numero inteiro: " );
    scanf("%i", &operador1);
    while(getchar() != '\n');

    prinf("Entre com outro numero inteiro: ");
    scanf("%i", &operador2);
    while(getchar() != '\n');
    
}